#include <stdio.h>

int main(){

    if (strcmp(all[i].opc,"hlt")==0){

        if(i == sizeof(all)-1){
            return;
        }else{
            return "Use hlt only at the end.";
        }                   
    }
}
